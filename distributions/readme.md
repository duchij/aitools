# 18F-FDG, 18F-CHOLIN, 18F-DOPA Distributions

- The structure of the dataset it quite simple. Export PET series into numpy arrays into corresponding folders as defined for the Python script.
- Define the folders for each stage
- Run the distribution_preparation script -> the final output is in folder of the Stage 3
- The zip the folder and upload it to Google drive, then use the distribution_learning for training your model.